class CCircle
{  double pi;
    double radius;
    double getRadius()
    {  return radius;
    }
    //返回半径
    void setCircle(double r, double p)
    {   pi=p;
        radius=r;
    }
    //用来设置pi和半径两个的值
}
